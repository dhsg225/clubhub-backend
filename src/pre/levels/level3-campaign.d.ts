/**
 * LEVEL_3 — Campaign / schedule resolution.
 *
 * Constitutional authority: PRE-REFERENCE-IMPLEMENTATION-v1.md §5.3, §5.5
 */
import type { PRE_Input, ScheduleRecord } from '../types';
import type { LevelResult } from './types';
export declare function resolveLevel3(input: PRE_Input, schedules: ScheduleRecord[]): LevelResult;
//# sourceMappingURL=level3-campaign.d.ts.map