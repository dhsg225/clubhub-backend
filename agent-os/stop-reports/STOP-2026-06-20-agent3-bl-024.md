# STOP REPORT

**Date**: 2026-06-20
**Stopped by**: Feature Development (Agent 3)

## Reason For Stopping

[x] Batch Objective Completed

## Description

BL-024 (Zone support: layout_template + zone_name) complete end-to-end, deployed to production.

### What was built

**`backend/db/migrate_010.sql`**
- `ALTER TABLE schedules ADD COLUMN IF NOT EXISTS zone_name VARCHAR(40) NOT NULL DEFAULT 'main'`
- `ALTER TABLE screens ADD COLUMN IF NOT EXISTS layout_template VARCHAR(40) NOT NULL DEFAULT 'fullscreen'`
- Applied to production DB. Column presence verified: `playlist_id`, `zone_name`, `layout_template` all confirmed via information_schema query.

**`backend/src/routes/schedules.js`**
- POST destructuring: added `zone_name = 'main'` default
- POST validation: zone_name must be non-empty string, max 40 chars
- INSERT: added `zone_name` as 14th column/param (`$14`)
- GET: `s.*` already returns `zone_name` — no change needed

**`backend/src/routes/screens.js`**
- Added `PATCH /screens/:id` route (placed after `PATCH /:id/heartbeat` so heartbeat literal match wins)
- Validates `layout_template` is one of: `fullscreen`, `split_horizontal`, `news_bar`, `quad`
- Returns 400 if missing/invalid, 404 if screen not found, 200 with updated row on success

**`apps/cms-web/src/lib/api-client.ts`**
- Added `patch` convenience wrapper: `patch: <T>(path, body) => apiRequest<T>('PATCH', path, body)`

**`apps/cms-web/src/routes/ScheduleCreator.tsx`**
- Added `LAYOUT_ZONES`, `LAYOUT_LABELS`, `ZONE_LABELS` constants
- Added `layoutTemplate` state (default `'fullscreen'`) and `zoneName` state (default `'main'`)
- New "Layout & zone" section between Priority and Date Window:
  - Layout template `<select>`: Full Screen / Split Horizontal / News Bar / Quad
  - Zone `<select>` (only shown when layout has >1 zone) populated from `LAYOUT_ZONES[layoutTemplate]`
  - Zone resets to first available zone when layout changes
- `zone_name: zoneName` added to mutation payload

**`apps/cms-web/src/routes/VenueDashboard.tsx`**
- Added `layout_template: string | null` to `Screen` interface
- Added `LAYOUT_OPTIONS` const and `LAYOUT_LABELS` record
- Added imports: `useMutation`, `useQueryClient` from `@tanstack/react-query`; `useState` from `react`
- Added `layoutSelections` state map (optimistic UI, keyed by screen id)
- Added `layoutErrors` state map (per-screen error messages on patch failure)
- Added `patchLayout` mutation: `PATCH /screens/:id` with `{ layout_template }`
  - On success: invalidates `['screens', venueId]` query
  - On error: reverts selection to original value, sets per-screen error message
- Added `handleLayoutChange` function
- `ScreenRow` now receives `layoutSelection`, `onLayoutChange`, `layoutError` props
- Layout column added to table: `<select>` with all 4 options, compact style (`0.8rem` font, `0.2rem 0.4rem` padding)
- Per-screen error message renders below select in red when patch fails

### Typecheck

`pnpm --filter @clubhub/cms-web typecheck`: **0 errors**

### Build

`pnpm --filter @clubhub/cms-web build`: **118 modules, 0 errors**
- `ScheduleCreator-Cf-tYqQu.js`, `ScheduleList-DI7D2UsT.js`, `VenueDashboard-CKvCuHig.js` confirmed in `public/assets/` ✓

### Production deployment

1. `migrate_010.sql` applied via Node pg client — `zone_name` and `layout_template` columns confirmed ✓
2. Also confirmed `playlist_id` (from BL-F07) was present — no re-application needed ✓
3. `schedules.js` + `screens.js` deployed to `/var/www/clubhub-cms.productionhouse.asia/src/routes/` via `sudo cp` ✓
4. Backend restarted with `SIGUSR2` → new PID 3215589 confirmed ✓
5. Frontend built (118 modules, 0 errors) → rsync'd via `/tmp/cms-dist/` → `public/` ✓
6. `ScheduleCreator-Cf-tYqQu.js`, `ScheduleList-DI7D2UsT.js`, `VenueDashboard-CKvCuHig.js` confirmed in `public/assets/` ✓

Note: BL-F07's `migrate_009.sql` (playlist_id on schedules) was also confirmed applied as part of this session's DB verification — it had been pending deployment but was already on the DB.

## Could This Have Been Avoided? N/A — clean delivery.

## INTERRUPTION COST

Estimated Time Lost: 0 minutes
Affected Backlog Items: BL-024
Workstream Impact: Current task only
Severity: Low

## Current State

**Completed this session**: All 8 steps from the brief. Full end-to-end delivery including production deploy.

**Decisions made**:
- `PATCH /screens/:id` placed after `/:id/heartbeat` in route order — Express matches the more specific literal `/heartbeat` suffix first since it's registered earlier.
- `patch` method added to api-client convenience wrappers (was missing; no other touch to api-client.ts logic).

## Recommended Next Action

BL-F08 — Image upload + server-side WebP conversion at 1920×1080. Required before any card type can use images in production. No UI dependency blockers remain.
