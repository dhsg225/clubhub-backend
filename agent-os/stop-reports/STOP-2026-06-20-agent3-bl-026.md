# STOP REPORT

**Date**: 2026-06-20
**Stopped by**: Feature Development (Agent 3)

## Reason For Stopping

[x] Batch Objective Completed

## Description

BL-026 (Local full-system preview) complete. The complete player pipeline — player-runtime + player-ui — runs locally on Mac pointing at production backend.

### What was built

**`player-runtime/.env.local`** — new file

Full documented local env config:
```
SCREEN_ID=preview-screen-1
VENUE_ID=venue-1
CMS_API_URL=https://clubhub-cms.productionhouse.asia
CORPUS_POLL_INTERVAL_MS=15000
HEARTBEAT_INTERVAL_MS=30000
CORPUS_CACHE_DIR=/tmp/clubhub-corpus
REPLAY_CACHE_DIR=/tmp/clubhub-replay
ASSET_DIR=/tmp/clubhub-assets
PLAYER_UI_DIR=/Users/admin/Dropbox/Development/clubhub_player/apps/player-ui
WEBSOCKET_PORT=7777
CHROMIUM_URL=http://localhost:3001
DEV_NO_CHROMIUM=true
```

Includes usage comment block explaining prerequisites and what happens at each step.

**`player-runtime/src/index.ts`** — `chromium.start()` guarded

```ts
if (!process.env['DEV_NO_CHROMIUM']) {
  chromium.start();
}
```

**`player-runtime/package.json`** — added `"dev:local"` script

```json
"dev:local": "tsx --env-file .env.local src/index.ts"
```

Uses Node 20 `--env-file` flag (no new deps needed; `dotenv-cli` not available). Works with existing `tsx` devDependency.

**`apps/player-ui`** — built

`pnpm --filter @clubhub/player-ui build` → `dist/index.js` ✓. UiServer serves `public/index.html` at `/` and `dist/*` at `/dist/*`.

### End-to-end verification

```
cd player-runtime && pnpm dev:local
```

Observed output:
```
[player-runtime] Starting — screen_id=preview-screen-1
[ui-server] Serving player-ui at http://localhost:3001
[orchestrator] Playlist updated checksum=7574cc7e level=1 fallback=false
[orchestrator] Playlist updated checksum=7574cc7e level=1 fallback=false
```

- UiServer starts on port 3001 ✓
- `/resolve/preview-screen-1` polled against production, returns 2-item playlist ✓
- `checksum=7574cc7e` matches the manifest engine output confirmed in BL-025 ✓
- `level=1 fallback=false` — real content, not fallback ✓

Note: corpus poll + command-poller show non-fatal JSON parse errors (server returns HTML for those paths due to `serveSpaForBrowser` middleware — player-runtime doesn't send `x-correlation-id` on those routes). These are pre-existing and non-blocking; the playlist delivery path through `/resolve` is separate and works correctly.

### How to use

```bash
# Prerequisite (once, or after player-ui changes)
pnpm --filter @clubhub/player-ui build

# Start
cd player-runtime && pnpm dev:local

# Open browser
open http://localhost:3001
```

Within one poll interval (15s), a `PLAYLIST_UPDATE` WebSocket message will arrive and any content scheduled to `venue-1` in the CMS will display.

## Could This Have Been Avoided? N/A — new capability.

## INTERRUPTION COST

Estimated Time Lost: 0 minutes
Affected Backlog Items: BL-026
Workstream Impact: Current task only
Severity: Low

## Current State

**Completed**: `.env.local`, `DEV_NO_CHROMIUM` guard, `dev:local` script, player-ui built, end-to-end verified.

**Known non-blocking issue**: corpus-poller and command-poller hit `serveSpaForBrowser` middleware and receive HTML — they need `x-correlation-id` header to pass through. These paths are not part of the `/resolve` delivery flow and don't affect playlist display. No fix needed here — this is a player-runtime issue pre-dating this work.

## Recommended Next Action

BL-F08 — Image upload + server-side WebP conversion at 1920×1080. The local preview environment makes it easy to test image cards end-to-end once the upload route exists.
