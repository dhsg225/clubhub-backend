# STOP REPORT

**Date**: 2026-06-19
**Stopped by**: Feature Development (Agent 3)

## Reason For Stopping

[x] Batch Objective Completed

## Description

BL-021 — Card Authoring Form complete. All acceptance criteria met.

### What was built

**`apps/cms-web/src/routes/ContentNew.tsx`** (~700 lines):

Split-panel layout (CSS grid, 380px form + fluid preview):

**Left panel — form:**
- Template type selector (5 options) — changing type resets form data to defaults
- 5 template-specific field sets:
  - `promo_slide`: title (45), subtitle (80), background_color (colour picker, default #1a1a2e), text_color (colour picker, default #fff)
  - `event_banner`: event_name (50), date (date input), time (time input), description (120)
  - `sponsor_banner`: sponsor_name (40), tagline (80), tier (Platinum/Gold/Silver select)
  - `menu_board`: up to 2 sections × 4 items each (section_title 30, item name 30, price 10) — add/remove controls
  - `daily_specials`: headline (30), up to 5 items (dish_name 30, price 10) — add/remove controls
- Character count shown below each text field (`{current}/{max}`, red when over limit)
- Expiry date field (required) + "No expiry" checkbox (disables date input)
- Client-side validation before submit: required fields, char limits, expiry presence
- `useMutation` → `POST /content` → `navigate('/campaigns')` on success
- Loading/error states on save button

**Right panel — live preview:**
- `paddingBottom: 56.25%` relative container for true 16:9 aspect ratio
- `LivePreview` component mirrors form state in real time (no submit needed)
- Same colour palette as `ContentPreview.tsx`: violet/orange/green/blue/red per type
- `promo_slide` uses form's background_color + text_color — operators see their colour choices live
- Template type badge top-left, "PREVIEW" watermark top-right (matches STUB convention)
- Field entries rendered as key/value rows beneath title
- "Fill in fields to preview" placeholder when no entries

**Other changes:**
- `apps/cms-web/src/App.tsx`: `/content/new` route added **before** `/content/:id` (prevents `:id` matching literal "new")
- `apps/cms-web/src/routes/CampaignList.tsx`: "New campaign" button (blue, top-right of header) linking to `/content/new`

### Decisions made

1. **`expires_at` in data JSONB** — no `expires_at` column in the `content` table schema. Rather than add a migration (out of scope for this task), `expires_at` is stored inside the `data` JSONB blob. Operators see it in the ContentDetail data fields table. A future task should add a dedicated DB column and move it out of JSONB for queryability.

2. **Inline validation only** — server-side 400 errors are surfaced via the mutation error display. No field-level inline errors to keep the component size manageable.

3. **No `Outlet` import conflict** — App.tsx already had `Outlet` imported from the previous session.

## Could This Have Been Avoided? N/A — clean delivery.

## INTERRUPTION COST

Estimated Time Lost: 0 minutes
Affected Backlog Items: BL-021
Workstream Impact: Current task only
Severity: Low

## Current State

**Completed this session**:
- `apps/cms-web/src/routes/ContentNew.tsx` (new, ~700 lines)
- `apps/cms-web/src/App.tsx` — `/content/new` route added
- `apps/cms-web/src/routes/CampaignList.tsx` — "New campaign" button added
- `agent-os/BACKLOG.md` — BL-021 filed and marked DONE

`pnpm --filter @clubhub/cms-web typecheck`: 0 errors

**Incomplete**: Nothing from this batch.

## Recommended Next Action

Per D-013 (Card → Playlist → Schedule → Screen), card authoring is done. Next layer:

**BL-022 — Playlist Composer** (`/playlists/new`): UI to assemble ordered cards into a playlist. The `/playlist` backend route exists. Each card gets a duration field. Drag-to-reorder is explicitly prohibited (D-013).

**BL-023 — Schedule Creator** (`/schedules/new`): Map a playlist to venue/screen group with daypart window. The `/schedules` backend route exists.

The `expires_at` DB column migration is also a recommended follow-up (move out of JSONB for queryability in the schedule engine).
