apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: clubhub
  labels:
    app: api-gateway
    app.kubernetes.io/part-of: clubhub
spec:
  selector:
    app: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
