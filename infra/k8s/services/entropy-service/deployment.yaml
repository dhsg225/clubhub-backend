apiVersion: apps/v1
kind: Deployment
metadata:
  name: entropy-service
  namespace: clubhub
  labels:
    app: entropy-service
    app.kubernetes.io/part-of: clubhub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: entropy-service
  template:
    metadata:
      labels:
        app: entropy-service
        app.kubernetes.io/part-of: clubhub
    spec:
      containers:
        - name: entropy-service
          image: clubhub/entropy-service:latest
          ports:
            - containerPort: 3003
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: clubhub-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: clubhub-secrets
                  key: REDIS_URL
          envFrom:
            - configMapRef:
                name: clubhub-config
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
      terminationGracePeriodSeconds: 30
