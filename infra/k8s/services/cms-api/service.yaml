apiVersion: v1
kind: Service
metadata:
  name: cms-api
  namespace: clubhub
  labels:
    app: cms-api
    app.kubernetes.io/part-of: clubhub
spec:
  selector:
    app: cms-api
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
