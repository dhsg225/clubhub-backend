apiVersion: apps/v1
kind: Deployment
metadata:
  name: replay-service
  namespace: clubhub
  labels:
    app: replay-service
    app.kubernetes.io/part-of: clubhub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: replay-service
  template:
    metadata:
      labels:
        app: replay-service
        app.kubernetes.io/part-of: clubhub
    spec:
      containers:
        - name: replay-service
          image: clubhub/replay-service:latest
          ports:
            - containerPort: 3002
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: clubhub-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: clubhub-secrets
                  key: REDIS_URL
          envFrom:
            - configMapRef:
                name: clubhub-config
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
      terminationGracePeriodSeconds: 60
