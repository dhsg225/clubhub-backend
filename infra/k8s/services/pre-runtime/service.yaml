apiVersion: v1
kind: Service
metadata:
  name: pre-runtime
  namespace: clubhub
  labels:
    app: pre-runtime
    app.kubernetes.io/part-of: clubhub
spec:
  selector:
    app: pre-runtime
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
