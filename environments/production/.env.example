NODE_ENV=production
DATABASE_URL=  # REQUIRED: injected by secrets manager
CORPUS_PUBLISHER_URL=  # REQUIRED: injected by secrets manager

# All service URLs are REQUIRED in production — injected by secrets manager
CMS_API_URL=
REPLAY_SERVICE_URL=
ENTROPY_SERVICE_URL=

# Metrics (required in production)
METRICS_DEBUG=false
